<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
    <%# @feed_items.each do |f| %>
    	<%# render f %>
    <%# end %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>