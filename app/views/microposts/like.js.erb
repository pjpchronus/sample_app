      	<% content = form_for(@other_mp, url: unlike_micropost_url(@other_mp), remote: true) do |f|%>
    			<%= f.submit "Liked! | #{@other_mp.get_likes.size}", class: "btn btn-primary", style: "width: 10%; font: bold 11px Arial;
      text-decoration: none;  background-color: #337ab7;  color: #ffffff;  padding: 2px 6px 2px 6px;" %>
    		<% end %>
$("#edit_micropost_<%= @other_mp.id %>").replaceWith('<%= j(content) %>');