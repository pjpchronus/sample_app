      	<% content = form_for(@other_mp, url: like_micropost_url(@other_mp), remote: true) do |f|%>
    			<%= f.submit "Like | #{@other_mp.get_likes.size}", class: "btn", style: "width: 10%; font: bold 11px Arial;
      text-decoration: none;  color: #111111;  padding: 2px 6px 2px 6px;" %>
				<% if current_user?(@other_mp.user) %>
					<%= link_to "Delete", @other_mp, method: :delete, data: { confirm: "You sure?" }, class: "btn btn-danger btn-xs", style: "height: 16px; vertical-align:  text-bottom;margin-bottom: -6px; padding-top: 0px 14px;" %>
				<% end %>
    		<% end %>
$("#edit_micropost_<%= @other_mp.id %>").replaceWith('<%= j(content) %>');